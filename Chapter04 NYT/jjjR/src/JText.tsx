import * as React from 'react';
import { Text, View } from 'react-native';

interface JTextProps {
}

const JText: React.SFC<JTextProps> = (props) => {
  const { 
   } = props
  return (
    <Text {...props}></Text>
  );
};